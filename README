INFORME:

1. se definieron las funciones mprotect y munprotect en el archivo sysproc.c para gestionar llamadas de usuario
    estas funciones llaman a otras funciones implementadas en vm.c para realizar la logica de proteccion

2. modificamos vm.c para que tengas las funciones mprotect que activa el modo solo lectura y munprotect que permite la escritura nuevamente

3. todo esto tiene que ser registrado en syscall.c

4. creamos el programa de prueba para comprobar que funciona el mprotect y sysprotect.

Dificultades:
problemas con las llamadas en syscall.c ya que inicial mente se usaba int y no uint64 lo que generaba error al llamar algo que estaba en int cuando solo lee el otro.
el addr dio muchos problemas para funcionar debido al que inicialmente llamaba datos que la logica escrita no permitia por lo que habia que reescribir ese codigo.



