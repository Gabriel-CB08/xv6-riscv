Informe Tarea 4:

1. Modificamos la estrucutra inode en fs.h para permitir el almacenamiento de archivos.

2. Agregamos sys_open en sysfile.c para validar los permisos.

3. Implementamos chmod para poder modificar los permismos de un archivo, con llamadas en syscall.h, syscall.c.

4. Creamos el test_chmod.c para verificar que funcionen las operaciones pedidas en el enunciado.

Dificultades encontradsa:

Encontre dificultad al crear variabel que no estaban definidas, la logica de permisos me daba errores y las validacioens de algunos archivos que necesitaban modificar el include para que estos pudieran funcionar en mi computador.